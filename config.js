// Global configuration to be loaded before all other scripts
window.SCREENPIPE_CONFIG = {
  disableAnalytics: true,
  disableSSE: true,
  disablePostHog: true, 
  disableReporting: true,
  disableErrorLogging: true,
  disable_external_dependency_loading: true
};
